# ระบบความปลอดภัยของเว็บไซต์

เอกสารนี้อธิบายมาตรการความปลอดภัยทุกข้อของโครงงาน **เว็บไซต์วางแผนการอ่านหนังสือและติดตามเป้าหมายการเรียน**
ว่าป้องกันอะไร ทำงานอย่างไร อยู่ที่ไฟล์ไหน และมีชุดทดสอบข้อใดพิสูจน์ว่าใช้งานได้จริง

เว็บไซต์นี้เก็บข้อมูลส่วนตัวของนักเรียน (ตารางอ่านหนังสือ เป้าหมายการเรียน) จึงต้องมั่นใจว่า
**ไม่มีใครเข้าถึงข้อมูลของคนอื่นได้** และ **บัญชีของนักเรียนถูกขโมยไปได้ยาก**

> ทุกมาตรการในเอกสารนี้มีชุดทดสอบอัตโนมัติที่ **จำลองการโจมตีจริง** แล้วตรวจว่าระบบปฏิเสธถูกต้อง
> รันได้ด้วยคำสั่ง `npm test` (ทั้งหมด 52 รายการ เป็นการทดสอบความปลอดภัย 35 รายการ)

---

## สารบัญ

1. [การเก็บรหัสผ่าน](#1-การเก็บรหัสผ่าน)
2. [การจัดการ session และคุกกี้](#2-การจัดการ-session-และคุกกี้)
3. [การป้องกัน CSRF](#3-การป้องกัน-csrf)
4. [การป้องกันการเข้าถึงข้อมูลของผู้อื่น (IDOR)](#4-การป้องกันการเข้าถึงข้อมูลของผู้อื่น-idor)
5. [การป้องกัน SQL Injection](#5-การป้องกัน-sql-injection)
6. [การป้องกัน XSS](#6-การป้องกัน-xss)
7. [การตรวจสอบข้อมูลนำเข้า](#7-การตรวจสอบข้อมูลนำเข้า)
8. [การจำกัดอัตราการเรียกใช้และการล็อกบัญชี](#8-การจำกัดอัตราการเรียกใช้และการล็อกบัญชี)
9. [การป้องกัน Path Traversal](#9-การป้องกัน-path-traversal)
10. [Security headers](#10-security-headers)
11. [การจัดการความลับและข้อมูลส่วนบุคคล](#11-การจัดการความลับและข้อมูลส่วนบุคคล)
12. [การป้องกันการโจมตีให้ทรัพยากรหมด](#12-การป้องกันการโจมตีให้ทรัพยากรหมด)
13. [สรุปตารางเชื่อมโยงมาตรการกับชุดทดสอบ](#13-สรุปตารางเชื่อมโยงมาตรการกับชุดทดสอบ)
14. [สิ่งที่ยังไม่ได้ทำและเหตุผล](#14-สิ่งที่ยังไม่ได้ทำและเหตุผล)

---

## 1. การเก็บรหัสผ่าน

**ไฟล์:** `server/security/password.js`

### ปัญหาที่ป้องกัน
ถ้าเก็บรหัสผ่านเป็นข้อความธรรมดา แล้ววันหนึ่งฐานข้อมูลรั่วไหล ผู้โจมตีจะได้รหัสผ่านของนักเรียนทุกคนทันที
และเนื่องจากคนส่วนใหญ่ใช้รหัสผ่านซ้ำกันหลายเว็บ ความเสียหายจะลามไปถึงบัญชีอื่น ๆ ด้วย

### วิธีที่ใช้

ระบบ **ไม่เคยเก็บรหัสผ่านตัวจริง** แต่เก็บเป็นค่าแฮชด้วย **scrypt**

```
รูปแบบที่เก็บ:  scrypt$16384$8$1$<salt>$<hash>
พารามิเตอร์:    N = 16384, r = 8, p = 1, ความยาวผลลัพธ์ 64 ไบต์
```

- **scrypt เป็นอัลกอริทึมแบบ memory-hard** คือบังคับให้ต้องใช้หน่วยความจำ 16 MB ต่อการคำนวณ 1 ครั้ง
  ทำให้ผู้โจมตีที่ใช้การ์ดจอเดารหัสผ่านทีละล้านครั้งทำได้ยากมาก เพราะติดข้อจำกัดเรื่องแรม
  ไม่เหมือนอัลกอริทึมเก่าอย่าง MD5 หรือ SHA-1 ที่คำนวณเร็วจนเดาได้เป็นพันล้านครั้งต่อวินาที
- **สุ่ม salt ใหม่ 16 ไบต์ทุกครั้ง** ผู้ใช้ 2 คนที่ตั้งรหัสผ่านเหมือนกันจะได้ค่าแฮชคนละค่า
  ตารางแฮชสำเร็จรูป (rainbow table) จึงใช้ไม่ได้
- **เปรียบเทียบด้วย `crypto.timingSafeEqual`** ซึ่งใช้เวลาคงที่เสมอไม่ว่าค่าจะตรงกันกี่ไบต์
  ป้องกันการโจมตีที่อาศัยการจับเวลา (timing attack)
- **เก็บพารามิเตอร์ไว้ในสตริงด้วย** ทำให้อนาคตปรับความแข็งแรงขึ้นได้ โดยที่รหัสผ่านเดิมยังใช้งานได้ต่อไป

### นโยบายรหัสผ่าน

- ยาว 10–128 ตัวอักษร
- ห้ามเป็นรหัสผ่านที่พบบ่อย (มีบัญชีดำในระบบ)
- ห้ามมีชื่อผู้ใช้อยู่ภายใน
- ต้องมีอักขระที่ต่างกันอย่างน้อย 4 แบบ (กัน `aaaaaaaaaa`)
- ต้องผสมอย่างน้อย 2 ประเภท เช่น ตัวอักษรกับตัวเลข

### การไม่บอกใบ้ว่ามีบัญชีนี้อยู่จริงหรือไม่

เมื่อเข้าสู่ระบบไม่สำเร็จ ระบบตอบข้อความเดียวกันเสมอ คือ *"ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง"*
ไม่ว่าจะเป็นกรณีไม่มีบัญชีนั้น หรือมีบัญชีแต่รหัสผ่านผิด

ที่สำคัญกว่านั้นคือ **ใช้เวลาตอบเท่ากันด้วย** — หากไม่พบชื่อผู้ใช้ ระบบจะเรียกฟังก์ชัน `fakeVerify()`
เพื่อแฮชหลอกให้เสียเวลาเท่ากับกรณีปกติ มิฉะนั้นผู้โจมตีจะจับเวลาตอบกลับ
แล้วไล่หาได้ว่าชื่อผู้ใช้ใดมีอยู่ในระบบจริงบ้าง

---

## 2. การจัดการ session และคุกกี้

**ไฟล์:** `server/security/session.js`

### วิธีที่ใช้

| มาตรการ | รายละเอียด |
|---|---|
| **token สุ่มด้วย CSPRNG** | ขนาด 32 ไบต์ (256 บิต) จาก `crypto.randomBytes` เดาไม่ได้ในทางปฏิบัติ |
| **ไม่เก็บ token ตัวจริงในฐานข้อมูล** | เก็บเฉพาะค่า **HMAC-SHA256** ของ token ถ้าฐานข้อมูลรั่ว ผู้โจมตียังปลอม session ไม่ได้เพราะต้องมีกุญแจลับด้วย |
| **หมดอายุ 2 ชั้น** | ไม่ใช้งาน 7 วัน (idle) และหมดอายุเด็ดขาด 30 วัน (absolute) |
| **สร้าง session ใหม่ทุกครั้งที่เข้าสู่ระบบ** | ป้องกัน session fixation คือการที่ผู้โจมตียัด session id ให้เหยื่อล่วงหน้าแล้วรอสวมรอย |
| **เปลี่ยนรหัสผ่านแล้วเตะอุปกรณ์อื่นออกทั้งหมด** | เผื่อกรณีที่เปลี่ยนเพราะสงสัยว่ามีคนอื่นเข้าถึงบัญชีได้ |
| **ผู้ใช้จัดการอุปกรณ์เองได้** | หน้าตั้งค่ามีรายการอุปกรณ์ที่เข้าสู่ระบบอยู่ กดออกจากระบบเครื่องใดก็ได้ |

### คุณสมบัติของคุกกี้

```
sp_session=<token>; Path=/; SameSite=Strict; HttpOnly; Secure; Max-Age=2592000
```

| คุณสมบัติ | ป้องกันอะไร |
|---|---|
| `HttpOnly` | JavaScript อ่านคุกกี้นี้ไม่ได้ ต่อให้มี XSS หลุดรอดมาก็ขโมย session ไปไม่ได้ |
| `SameSite=Strict` | เบราว์เซอร์ไม่ส่งคุกกี้นี้ไปกับคำขอที่มาจากเว็บอื่น เป็นการกัน CSRF ชั้นแรก |
| `Secure` | ส่งผ่าน HTTPS เท่านั้น กันการดักฟังบนเครือข่าย (เปิดอัตโนมัติเมื่อ `NODE_ENV=production`) |
| `__Host-` prefix | เมื่อใช้งานจริงบน HTTPS ชื่อคุกกี้จะขึ้นต้นด้วย `__Host-` ซึ่งบังคับให้เบราว์เซอร์ยอมรับเฉพาะคุกกี้ที่ปลอดภัยเต็มรูปแบบ และกันไม่ให้ subdomain อื่นเขียนทับได้ |

---

## 3. การป้องกัน CSRF

**ไฟล์:** `server/security/csrf.js`

### ปัญหาที่ป้องกัน

CSRF (Cross-Site Request Forgery) คือการที่ผู้โจมตีสร้างเว็บไซต์ปลอมขึ้นมา แล้วหลอกให้นักเรียนที่กำลัง
เข้าสู่ระบบเว็บเราอยู่กดเข้าไป เว็บปลอมนั้นจะสั่งให้เบราว์เซอร์ยิงคำขอมาที่เว็บเรา
พร้อมคุกกี้ของเหยื่อไปด้วย ทำให้เกิดการกระทำที่เจ้าตัวไม่ได้ตั้งใจ เช่น ลบแผนการอ่านทิ้งทั้งหมด

### ป้องกัน 4 ชั้น

**ชั้นที่ 1 — `SameSite=Strict`**
เบราว์เซอร์สมัยใหม่จะไม่ส่งคุกกี้ไปกับคำขอที่มาจากเว็บอื่นเลย

**ชั้นที่ 2 — ตรวจ header `Origin` / `Referer`**
ทุกคำขอที่เปลี่ยนแปลงข้อมูล (POST, PATCH, DELETE) ต้องมาจาก origin ของเราเท่านั้น
ถ้าไม่มีทั้ง `Origin` และ `Referer` ก็ปฏิเสธ

**ชั้นที่ 3 — Double-submit token**
ระบบออก CSRF token สุ่ม 32 ไบต์ใส่คุกกี้ที่ JavaScript อ่านได้ แล้วหน้าเว็บต้องอ่านค่านั้น
ส่งกลับมาทาง header `X-CSRF-Token` เซิร์ฟเวอร์จะเทียบว่าตรงกันหรือไม่ด้วยวิธีที่ใช้เวลาคงที่
เว็บของผู้โจมตีอ่านคุกกี้ข้ามโดเมนไม่ได้ จึงเดา token ไม่ถูก

**ชั้นที่ 4 — บังคับ `Content-Type: application/json`**
ฟอร์ม HTML ธรรมดาส่ง `Content-Type` แบบนี้ไม่ได้ ผู้โจมตีจึงยิงด้วยฟอร์มธรรมดาไม่สำเร็จ

---

## 4. การป้องกันการเข้าถึงข้อมูลของผู้อื่น (IDOR)

**ไฟล์:** `server/routes/*.js` ทุกไฟล์

### ปัญหาที่ป้องกัน

IDOR (Insecure Direct Object Reference) คือช่องโหว่ที่พบบ่อยที่สุดอย่างหนึ่ง
เกิดเมื่อระบบเช็คแค่ว่า "เข้าสู่ระบบแล้วหรือยัง" แต่ลืมเช็คว่า "ข้อมูลชิ้นนี้เป็นของคนนี้จริงหรือเปล่า"

ถ้าเป็นแบบนั้น นักเรียนคนหนึ่งเพียงแค่เปลี่ยนเลขใน URL จาก `/api/plans/5` เป็น `/api/plans/6`
ก็จะเห็นตารางอ่านหนังสือของเพื่อนได้ทันที

### วิธีที่ใช้

**ทุกคำสั่ง SQL ที่แตะข้อมูลผู้ใช้ มีเงื่อนไข `WHERE user_id = ?` เสมอ โดยไม่มีข้อยกเว้น**

```sql
-- ตัวอย่างจาก server/routes/plans.js
DELETE FROM study_plans WHERE id = ? AND user_id = ?
```

และเมื่อไม่พบข้อมูล ระบบตอบ **404 (ไม่พบข้อมูล)** ไม่ใช่ 403 (ไม่มีสิทธิ์)
เพราะการตอบ 403 เท่ากับบอกผู้โจมตีว่า *"ข้อมูลชิ้นนี้มีอยู่จริงนะ แค่คุณไม่มีสิทธิ์"*
ซึ่งเป็นการรั่วไหลข้อมูลโดยไม่จำเป็น

นอกจากนี้ยังตรวจความเป็นเจ้าของแบบไขว้ด้วย เช่น เวลาสร้างแผนแล้วระบุ `subjectId`
ระบบจะตรวจก่อนว่ารายวิชานั้นเป็นของผู้ใช้คนนี้จริง ป้องกันการผูกข้อมูลข้ามบัญชี

---

## 5. การป้องกัน SQL Injection

**ไฟล์:** `server/db.js` และ `server/routes/*.js`

### ปัญหาที่ป้องกัน

ถ้านำข้อความจากผู้ใช้มาต่อเป็นคำสั่ง SQL ตรง ๆ ผู้โจมตีจะพิมพ์ข้อความที่ทำให้คำสั่งเปลี่ยนความหมายได้
เช่น พิมพ์ชื่อแผนว่า `'; DROP TABLE study_plans; --` แล้วตารางทั้งตารางจะหายไป

### วิธีที่ใช้

**ทุกคำสั่ง SQL ในโครงงานนี้เป็น prepared statement และส่งค่าผ่าน parameter binding เสมอ**
ไม่มีการนำข้อมูลจากผู้ใช้มาต่อเป็นสตริง SQL แม้แต่จุดเดียว

```js
// ถูกต้อง — ค่าถูกส่งแยกจากคำสั่ง ฐานข้อมูลจึงถือเป็น "ข้อมูล" เสมอ ไม่ใช่ "คำสั่ง"
sql('SELECT * FROM study_plans WHERE id = ? AND user_id = ?').get(id, userId);

// วิธีที่ห้ามทำเด็ดขาด และไม่มีอยู่ในโครงงานนี้
db.exec(`SELECT * FROM study_plans WHERE id = ${id}`);
```

### เรื่องการต่อสตริงในคำสั่ง SQL

โครงงานนี้ **มี** การใช้ template literal ต่อสตริงในคำสั่ง SQL อยู่บ้าง ตรวจดูได้ด้วยคำสั่ง

```bash
grep -rn 'sql(`\|db.exec(`' server/ | grep '\${'
```

จะพบทั้งหมด 16 จุด แต่ **ไม่มีจุดใดเลยที่นำข้อมูลจากผู้ใช้มาต่อ** โดยแบ่งเป็น 2 กลุ่ม

**กลุ่มที่ 1 (15 จุด) — ต่อกับค่าคงที่ที่ประกาศไว้ในไฟล์เดียวกัน**

เพื่อไม่ให้ต้องเขียนคำสั่ง `SELECT ... JOIN ...` ยาว ๆ ซ้ำหลายรอบ จึงแยกออกมาเป็นค่าคงที่
เช่น `SELECT_PLAN` ใน `routes/plans.js`, `SELECT_GOAL` ใน `routes/goals.js`
และ `MINUTES` ใน `routes/stats.js` ค่าเหล่านี้เป็นสตริงตายตัวในซอร์สโค้ด ผู้ใช้แก้ไม่ได้

```js
// ตัวอย่างจริงจาก routes/plans.js — SELECT_PLAN เป็นค่าคงที่
// ส่วนค่าจากผู้ใช้ (id, userId) ยังคงส่งผ่าน ? เหมือนเดิมทุกจุด
const row = sql(`${SELECT_PLAN} WHERE p.id = ? AND p.user_id = ?`).get(id, ctx.userId);
```

**กลุ่มที่ 2 (1 จุด) — `PRAGMA user_version` ในระบบ migration**

```js
db.exec(`PRAGMA user_version = ${version + 1}`);
```

ค่านี้มาจากความยาวของ array ในโค้ดเอง ไม่ได้มาจากผู้ใช้ และคำสั่ง PRAGMA ของ SQLite
ไม่รองรับ parameter binding อยู่แล้ว จึงจำเป็นต้องเขียนแบบนี้

**กฎที่ยึดจริงคือ: ค่าใดก็ตามที่มาจากผู้ใช้ ต้องผ่าน `?` เสมอ ไม่มีข้อยกเว้น**
ซึ่งตรวจสอบได้จากทุกจุดข้างต้นว่าไม่มี `${` ตัวใดผูกกับตัวแปรที่รับมาจากคำขอเลย

---

## 6. การป้องกัน XSS

**ไฟล์:** `public/js/dom.js` และ `server/security/headers.js`

### ปัญหาที่ป้องกัน

XSS (Cross-Site Scripting) คือการที่ผู้โจมตีแทรกโค้ด JavaScript เข้าไปในข้อมูล เช่น
ตั้งชื่อแผนการอ่านว่า `<script>ส่งคุกกี้ไปให้ผู้โจมตี</script>`
ถ้าหน้าเว็บนำข้อความนั้นไปแสดงแบบไม่ระวัง โค้ดจะถูกรันในเบราว์เซอร์ของทุกคนที่เห็นข้อความนั้น

### ป้องกัน 3 ชั้น

**ชั้นที่ 1 — ไม่ใช้ `innerHTML` กับข้อมูลผู้ใช้เลย**

ทั้งเว็บสร้างหน้าจอผ่านฟังก์ชันใน `public/js/dom.js` ซึ่งใส่ข้อความด้วย `textContent` เท่านั้น
เบราว์เซอร์จะถือว่าเป็น "ข้อความ" เสมอ ไม่ใช่ "โค้ด HTML"

```js
// จาก public/js/dom.js
} else if (key === 'text') {
  node.textContent = String(value);   // ปลอดภัยเสมอ
}
```

ตรวจสอบได้ด้วย

```bash
grep -rn 'innerHTML\|outerHTML\|insertAdjacentHTML\|document.write\|eval(' public/js/
```

จะพบเพียงบรรทัดเดียว คือคอมเมนต์ใน `dom.js` ที่อธิบายกฎข้อนี้เอง ไม่มีการเรียกใช้จริงแม้แต่จุดเดียว

**ชั้นที่ 2 — Content Security Policy**

```
default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self' data:;
font-src 'self'; connect-src 'self'; form-action 'self';
frame-ancestors 'none'; base-uri 'none'; object-src 'none'
```

CSP บอกเบราว์เซอร์ว่าอนุญาตให้รันสคริปต์จากโดเมนตัวเองเท่านั้น
**ไม่มี `unsafe-inline` และไม่มี `unsafe-eval`** ดังนั้นต่อให้ผู้โจมตีแทรกสคริปต์เข้ามาได้จริง
เบราว์เซอร์ก็จะปฏิเสธไม่รันให้อยู่ดี

ด้วยเหตุนี้เว็บนี้จึงไม่ใช้ CDN ไม่ใช้ Google Fonts และไม่มี `style="..."` ใน HTML แม้แต่ที่เดียว
(ตอนพัฒนาเคยมีอยู่ 1 จุด และถูกตรวจพบด้วยการทดสอบด้วยเบราว์เซอร์จริง จึงแก้เป็นคลาส CSS แล้ว)

**ชั้นที่ 3 — `X-Content-Type-Options: nosniff`**

บังคับให้เบราว์เซอร์เชื่อชนิดไฟล์ที่เราระบุ ไม่เดาเอง
ป้องกันกรณีที่ไฟล์ข้อความถูกตีความเป็นสคริปต์

---

## 7. การตรวจสอบข้อมูลนำเข้า

**ไฟล์:** `server/security/validate.js`

หลักการคือ **"ไม่เชื่อข้อมูลจากผู้ใช้เด็ดขาด"** ทุก field ต้องประกาศชนิด ความยาว และช่วงที่ยอมรับไว้ล่วงหน้า

| การตรวจ | รายละเอียด |
|---|---|
| **allowlist ไม่ใช่ blocklist** | field ที่ไม่ได้ประกาศไว้ใน schema จะถูกทิ้งทั้งหมด ผู้โจมตีจึงส่ง `userId` หรือ `id` มาแก้ค่าที่ไม่ควรแก้ไม่ได้ (mass assignment) |
| **ตรวจชนิดและช่วง** | ตัวเลขต้องอยู่ในช่วงที่กำหนด ข้อความต้องไม่ยาวเกิน ค่า enum ต้องอยู่ในรายการที่อนุญาต |
| **ตรวจวันที่ว่ามีจริง** | `2026-02-31` ถูกปฏิเสธ เพราะเดือนกุมภาพันธ์ไม่มีวันที่ 31 |
| **ตรวจเวลาและตรรกะ** | เวลาต้องอยู่ในรูปแบบ `ชช:นน` และเวลาสิ้นสุดต้องอยู่หลังเวลาเริ่มเสมอ |
| **ตัดอักขระควบคุม** | อักขระที่มองไม่เห็นถูกตัดออก ป้องกันการปลอมแปลงข้อความและการแทรกบรรทัดใน log |
| **กัน prototype pollution** | ผลลัพธ์สร้างด้วย `Object.create(null)` และปฏิเสธคีย์ `__proto__`, `constructor`, `prototype` |

นอกจากนี้ยังมีการ **จำกัดจำนวนข้อมูลต่อบัญชี** (รายวิชา 50, แผน 2,000, เป้าหมาย 300)
เพื่อไม่ให้ใครสร้างข้อมูลรัว ๆ จนฐานข้อมูลบวมและเปลืองพื้นที่ของโฮสต์

---

## 8. การจำกัดอัตราการเรียกใช้และการล็อกบัญชี

**ไฟล์:** `server/security/ratelimit.js`

### จำกัดตาม IP (token bucket)

| ประเภท | โควตาเริ่มต้น |
|---|---|
| ทุก endpoint รวมกัน | 240 คำขอต่อนาทีต่อ IP |
| เข้าสู่ระบบ / สมัครสมาชิก / เปลี่ยนรหัสผ่าน | 10 ครั้งต่อ 5 นาทีต่อ IP |

**จุดสำคัญเรื่องหน่วยความจำ:** ตัวนับเก็บใน `Map` ที่ **จำกัดจำนวนรายการสูงสุดไว้**
ถ้าผู้โจมตีปลอม IP เป็นล้าน ๆ ค่า `Map` จะไม่โตจนแรมหมด เพราะรายการเก่าสุดจะถูกลบทิ้งเมื่อเกินโควตา
และมีการกวาดรายการที่หมดอายุทิ้งเป็นระยะทุกชั่วโมง (มีชุดทดสอบยิง 5,000 IP ปลอมเพื่อพิสูจน์ข้อนี้)

### ล็อกบัญชีเมื่อเดารหัสผ่านผิด

- เดาผิดได้ 5 ครั้งก่อนเริ่มล็อก
- หลังจากนั้นล็อกแบบเพิ่มเวลาเป็นทวีคูณ: 1 นาที → 2 → 4 → 8 ... สูงสุด 1 ชั่วโมง
- **ล็อกทั้งตามชื่อผู้ใช้และตาม IP** ผู้โจมตีจึงหนีด้วยการเปลี่ยนชื่อผู้ใช้ไปเรื่อย ๆ ไม่ได้
- **เก็บตัวนับไว้ในฐานข้อมูล** การล็อกจึงยังมีผลแม้เซิร์ฟเวอร์รีสตาร์ท

### ความเชื่อถือ header `X-Forwarded-For`

ระบบจะอ่าน IP จริงจาก header นี้ **เฉพาะเมื่อเปิด `TRUST_PROXY` เท่านั้น**
เพราะถ้าเชื่อทุกกรณี ใครก็ปลอม header นี้เพื่อหนี rate limit ได้ง่าย ๆ

---

## 9. การป้องกัน Path Traversal

**ไฟล์:** `server/static.js`

### ปัญหาที่ป้องกัน

ผู้โจมตีขอ path แบบ `/../../server/config.js` เพื่ออ่านซอร์สโค้ดหรือไฟล์กุญแจลับของเซิร์ฟเวอร์

### ป้องกัน 5 ชั้น

1. ปฏิเสธ path ที่มี null byte หรือ backslash
2. `path.posix.normalize()` เพื่อยุบ `..` และ `.` ออกก่อน แล้วปฏิเสธถ้ายังเหลือ `..`
3. **ชั้นสำคัญที่สุด** — `path.resolve()` เป็น absolute path แล้วตรวจซ้ำว่ายังอยู่ใต้โฟลเดอร์ `public/` จริง
4. ปฏิเสธไฟล์หรือโฟลเดอร์ที่ขึ้นต้นด้วยจุด (`.env`, `.git`)
5. เสิร์ฟเฉพาะนามสกุลในรายการอนุญาต (`.html .css .js .svg .png .ico .webmanifest`) เท่านั้น

ชุดทดสอบยิงด้วยรูปแบบการโจมตี 6 แบบ รวมทั้งแบบเข้ารหัส URL (`%2e%2e`) และแบบซ้อน (`....//`)
แล้วตรวจว่าไม่มีรูปแบบใดคืนซอร์สโค้ดกลับมา

---

## 10. Security headers

**ไฟล์:** `server/security/headers.js` — ใส่ให้ **ทุก response** โดยไม่มีข้อยกเว้น

| Header | ป้องกันอะไร |
|---|---|
| `Content-Security-Policy` | XSS — จำกัดว่าโหลดและรันอะไรได้บ้าง |
| `X-Content-Type-Options: nosniff` | MIME sniffing — เบราว์เซอร์ต้องไม่เดาชนิดไฟล์เอง |
| `X-Frame-Options: DENY` | Clickjacking — ห้ามเว็บอื่นเอาเราไปใส่ iframe |
| `Referrer-Policy: same-origin` | ไม่ส่ง URL ของเราไปให้เว็บอื่นตอนผู้ใช้กดลิงก์ออก |
| `Permissions-Policy` | ปิดสิทธิ์กล้อง ไมโครโฟน ตำแหน่ง การชำระเงิน และ USB ทั้งหมด |
| `Cross-Origin-Opener-Policy` | แยกหน้าต่างเบราว์เซอร์ออกจากเว็บอื่น |
| `Cross-Origin-Resource-Policy` | ห้ามเว็บอื่นดึงทรัพยากรของเราไปใช้ |
| `Strict-Transport-Security` | บังคับใช้ HTTPS เสมอเป็นเวลา 1 ปี (เปิดเมื่อใช้งานจริง) |
| ไม่มี `X-Powered-By` | ไม่บอกผู้โจมตีว่าเราใช้เทคโนโลยีอะไร |

---

## 11. การจัดการความลับและข้อมูลส่วนบุคคล

| เรื่อง | วิธีจัดการ |
|---|---|
| **กุญแจลับ** | อ่านจาก env `SESSION_SECRET` ถ้าไม่มีจะสุ่มสร้างเก็บที่ `data/secret.key` สิทธิ์ไฟล์ `600` |
| **ไม่มีความลับในโค้ด** | ไม่มีรหัสผ่านหรือกุญแจใด ๆ ฝังอยู่ในซอร์สโค้ด |
| **ไม่หลุดขึ้น Git** | `.gitignore` ครอบคลุม `data/`, `*.db`, `*.key`, `.env` ไว้แล้ว |
| **ไม่เก็บ IP ตรง ๆ** | บันทึกเฉพาะค่าแฮชของ IP เก็บข้อมูลส่วนบุคคลเท่าที่จำเป็นจริง ๆ |
| **ไม่เก็บ User-Agent เต็ม** | แปลงเป็นชื่อสั้น ๆ เช่น "Chrome บน Android" พอให้ผู้ใช้จำอุปกรณ์ตัวเองได้ |
| **ไม่ log ความลับ** | ไม่มีการเขียนรหัสผ่าน token หรือคุกกี้ลง log เด็ดขาด |
| **ข้อความ error กลาง ๆ** | ผู้ใช้เห็นแค่ *"เกิดข้อผิดพลาดภายในระบบ"* ส่วนรายละเอียดจริงและ stack trace เขียนลง log ของเซิร์ฟเวอร์เท่านั้น |
| **บันทึกเหตุการณ์ความปลอดภัย** | เก็บประวัติเข้าสู่ระบบสำเร็จ/ล้มเหลว เปลี่ยนรหัสผ่าน ให้ผู้ใช้ตรวจสอบเองได้ในหน้าตั้งค่า |
| **ลบข้อมูลตามกัน** | ลบบัญชีแล้วข้อมูลทั้งหมดถูกลบตาม (`ON DELETE CASCADE`) |

---

## 12. การป้องกันการโจมตีให้ทรัพยากรหมด

| ภัย | มาตรการ |
|---|---|
| **ส่ง body ขนาดมหึมาให้แรมเต็ม** | จำกัดที่ 64 KB และ **หยุดเก็บข้อมูลทันทีที่เกิน** ไม่รอจนอ่านครบแล้วค่อยตรวจ พร้อมมีเพดานแข็งที่ตัดการเชื่อมต่อทิ้ง |
| **เปิดการเชื่อมต่อค้างไว้ (slowloris)** | ตั้ง `headersTimeout` และ `requestTimeout` ไว้ที่ 15 วินาที และจำกัดจำนวน header ที่ 60 |
| **แรมหมดเพราะแฮชรหัสผ่านพร้อมกัน** | scrypt ใช้แรม 16 MB ต่อครั้ง ถ้ามี 20 คำขอพร้อมกันจะต้องใช้ 320 MB ทันที ระบบจึง**จำกัดให้แฮชพร้อมกันได้แค่ 2 งาน** ทำให้แรมส่วนนี้มีเพดานตายตัวที่ราว 32 MB ไม่ว่าจะถูกยิงมากแค่ไหน |
| **ตัวนับ rate limit บวม** | `Map` มีเพดานจำนวนรายการและกวาดของเก่าทิ้งเป็นระยะ |
| **ฐานข้อมูลบวม** | จำกัดจำนวนข้อมูลต่อบัญชี และลบ session หมดอายุ / ประวัติเก่าทิ้งอัตโนมัติทุกชั่วโมง |
| **แรมของ SQLite** | ตั้ง `PRAGMA cache_size = -2000` จำกัด cache ไว้ที่ 2 MB |
| **ไฟล์ใหญ่กินแรม** | เสิร์ฟไฟล์ด้วย stream ไม่อ่านทั้งไฟล์เข้าหน่วยความจำ |

---

## 13. สรุปตารางเชื่อมโยงมาตรการกับชุดทดสอบ

รันทั้งหมดด้วย `npm test`

| # | มาตรการ | ไฟล์ที่ทำ | ชุดทดสอบที่พิสูจน์ |
|---|---|---|---|
| 1 | รหัสผ่านถูกแฮชด้วย scrypt + salt สุ่ม | `security/password.js` | *รหัสผ่านเดียวกันให้ค่าแฮชต่างกัน และตรวจสอบกลับได้* |
| 2 | ฐานข้อมูลไม่มีรหัสผ่านและ token แบบอ่านได้ | `security/password.js`, `security/session.js` | *ฐานข้อมูลไม่เก็บรหัสผ่านและ session token แบบอ่านได้* |
| 3 | นโยบายรหัสผ่าน | `security/password.js` | *รหัสผ่านที่อ่อนแอถูกปฏิเสธ*, *ฟังก์ชันตรวจความแข็งแรง...* |
| 4 | ไม่บอกใบ้ว่ามีบัญชีอยู่จริง | `routes/auth.js` | *ไม่บอกใบ้ว่ามีบัญชีนี้อยู่จริงหรือไม่* |
| 5 | คุกกี้ HttpOnly + SameSite=Strict | `security/session.js` | *คุกกี้ session ตั้ง HttpOnly และ SameSite=Strict* |
| 6 | session ที่เพิกถอนแล้วใช้ไม่ได้ | `security/session.js` | *session ที่ถูกเพิกถอนแล้วใช้ต่อไม่ได้* |
| 7 | เปลี่ยนรหัสผ่านแล้วเตะอุปกรณ์อื่น | `routes/auth.js` | *เปลี่ยนรหัสผ่านแล้วอุปกรณ์อื่นถูกเตะออกจากระบบ* |
| 8 | CSRF — ต้องมี token | `security/csrf.js` | *คำขอที่ไม่มี CSRF token ถูกปฏิเสธ*, *CSRF token ผิดถูกปฏิเสธ* |
| 9 | CSRF — ตรวจ Origin/Referer | `security/csrf.js` | *คำขอจากเว็บไซต์อื่น...*, *คำขอที่ไม่มีทั้ง Origin และ Referer...* |
| 10 | IDOR — แผนการอ่าน | `routes/plans.js` | *ผู้ใช้ B เปิดดู แก้ไข หรือลบแผนของผู้ใช้ A ไม่ได้* |
| 11 | IDOR — เป้าหมาย | `routes/goals.js` | *ผู้ใช้ B แก้ไขหรือลบเป้าหมายของผู้ใช้ A ไม่ได้* |
| 12 | IDOR — รายวิชา | `routes/subjects.js` | *ผู้ใช้ B แก้ไขหรือลบรายวิชาของผู้ใช้ A ไม่ได้* |
| 13 | IDOR — session ของผู้อื่น | `routes/auth.js` | *ผู้ใช้ B เพิกถอน session ของผู้ใช้ A ไม่ได้* |
| 14 | ตรวจความเป็นเจ้าของแบบไขว้ | `routes/plans.js` | *ผูกแผนเข้ากับรายวิชาของผู้ใช้อื่นไม่ได้* |
| 15 | รายการที่ดึงมามีเฉพาะของตัวเอง | ทุกไฟล์ใน `routes/` | *รายการที่ดึงมาต้องมีเฉพาะข้อมูลของตัวเอง* |
| 16 | SQL Injection | `db.js` + `routes/` | *ข้อความที่มีรูปแบบ SQL injection ถูกเก็บเป็นข้อความ ไม่ถูกนำไปรัน* |
| 17 | XSS | `public/js/dom.js` | *ข้อความที่มีสคริปต์ถูกเก็บเป็นข้อความธรรมดา ไม่ถูกดัดแปลง* |
| 18 | Mass assignment | `security/validate.js` | *field แปลกปลอมที่ไม่ได้ประกาศใน schema ถูกทิ้ง* |
| 19 | Prototype pollution | `security/validate.js` | *การโจมตีแบบ prototype pollution ไม่สำเร็จ* |
| 20 | จำกัดขนาด body | `http/body.js` | *body ที่ใหญ่เกินกำหนดถูกปฏิเสธด้วย 413* |
| 21 | บังคับ Content-Type | `http/body.js` | *Content-Type ที่ไม่ใช่ JSON ถูกปฏิเสธด้วย 415* |
| 22 | ตรวจชนิดและช่วงของข้อมูล | `security/validate.js` | *ค่าที่เกินช่วงที่กำหนดถูกปฏิเสธ*, *วันที่ไม่มีอยู่จริงถูกปฏิเสธ* |
| 23 | Path traversal | `static.js` | *การพยายามอ่านไฟล์นอกโฟลเดอร์ public ถูกปฏิเสธ* (6 รูปแบบ) |
| 24 | ไฟล์ซ่อนและนามสกุลนอกรายการ | `static.js` | *ไฟล์ซ่อนและนามสกุลที่ไม่อนุญาตเข้าถึงไม่ได้* |
| 25 | Security headers | `security/headers.js` | *security headers ครบถ้วนทั้งหน้าเว็บและ API* |
| 26 | ล็อกบัญชีเมื่อเดารหัสผ่านผิด | `security/ratelimit.js` | *บัญชีถูกล็อกชั่วคราวเมื่อเดารหัสผ่านผิดหลายครั้ง* |
| 27 | Rate limit ทำงานถูกต้อง | `security/ratelimit.js` | *ถังโทเคนปล่อยผ่านตามโควตาแล้วปฏิเสธส่วนเกิน* |
| 28 | ตัวนับ rate limit ไม่กินแรมไม่จำกัด | `security/ratelimit.js` | *จำนวน key ที่จำได้ถูกจำกัด...* (ยิงด้วย 5,000 IP ปลอม) |
| 29 | ต้องเข้าสู่ระบบก่อนใช้ API | `app.js` | *เข้าถึง API โดยไม่เข้าสู่ระบบต้องได้ 401* |

---

## 14. สิ่งที่ยังไม่ได้ทำและเหตุผล

การบอกว่ามีอะไรที่ **ยังไม่ได้ทำ** สำคัญพอ ๆ กับการบอกว่าทำอะไรไปแล้ว

| เรื่อง | สถานะ | เหตุผล |
|---|---|---|
| **ยืนยันตัวตน 2 ชั้น (2FA)** | ยังไม่มี | เป็นมาตรการที่ดีมาก แต่เกินขอบเขตโครงงานระดับมัธยมศึกษา และต้องใช้ไลบรารีเพิ่ม ซึ่งขัดกับหลักการไม่ใช้ dependency |
| **กู้คืนรหัสผ่านทางอีเมล** | ยังไม่มี | ต้องพึ่งบริการส่งอีเมลภายนอก ซึ่งมีค่าใช้จ่ายและเพิ่มความซับซ้อน ปัจจุบันถ้าลืมรหัสผ่านต้องให้ผู้ดูแลระบบช่วย |
| **เข้ารหัสข้อมูลในฐานข้อมูล** | ยังไม่มี | ข้อมูลตารางอ่านหนังสือไม่ใช่ข้อมูลอ่อนไหวระดับสูง และการเข้ารหัสจะทำให้ค้นหาและสรุปสถิติทำได้ยากมาก ส่วนรหัสผ่านนั้นถูกแฮชไว้แล้ว |
| **ตรวจรหัสผ่านกับฐานข้อมูลรหัสผ่านที่รั่วไหล** | ยังไม่มี | ต้องเรียก API ภายนอก (เช่น Have I Been Pwned) ซึ่งจะทำให้เว็บต้องพึ่งบริการภายนอก ปัจจุบันใช้บัญชีดำรหัสผ่านที่พบบ่อยแทน |
| **บันทึก log แบบรวมศูนย์** | ยังไม่มี | เกินความจำเป็นของโครงงานระดับนี้ ปัจจุบันเขียน log ลง stdout ซึ่งโฮสต์ส่วนใหญ่เก็บให้อยู่แล้ว |
| **สำรองข้อมูลอัตโนมัติ** | ยังไม่มี | ผู้ดูแลควรตั้งค่าเองตามโฮสต์ที่ใช้ (Fly.io มีระบบ snapshot ของ volume ให้อยู่แล้ว) |

### ข้อควรทำเมื่อนำไปใช้งานจริง

1. **ตั้ง `SESSION_SECRET` เองเสมอ** อย่าปล่อยให้ระบบสุ่มสร้าง เพราะถ้าไฟล์ `data/secret.key` หาย
   ผู้ใช้ทุกคนจะหลุดจากระบบพร้อมกัน
2. **ตั้ง `APP_ORIGIN` ให้ตรงกับที่อยู่จริง** เพื่อให้การตรวจ CSRF เข้มที่สุด
3. **ใช้ HTTPS เสมอ** และตั้ง `NODE_ENV=production` เพื่อให้คุกกี้เป็นแบบ Secure
4. **อัปเดต Node.js สม่ำเสมอ** เพราะแม้โครงงานนี้จะไม่มี dependency ภายนอก แต่ตัว Node.js เองก็มีการแก้ช่องโหว่เป็นระยะ

---

## การรายงานช่องโหว่

หากพบช่องโหว่ด้านความปลอดภัย กรุณาแจ้งคณะผู้จัดทำหรือครูที่ปรึกษาโครงงานโดยตรง
และโปรดอย่าเปิดเผยรายละเอียดต่อสาธารณะจนกว่าจะได้รับการแก้ไข
