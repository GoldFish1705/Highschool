# คู่มือนำเว็บไซต์ขึ้นใช้งานจริง

เอกสารนี้บอกวิธีทำให้เว็บไซต์เข้าถึงได้จากอินเทอร์เน็ต แบบทำตามได้ทีละขั้น

---

## สรุปสั้น ๆ ก่อน

| โฮสต์ | ใช้กับโครงงานนี้ได้ไหม | เหตุผล |
|---|---|---|
| **Render** | **ได้ — แนะนำ** | รัน Node ค้างไว้ได้ และเขียนไฟล์ได้ |
| **Fly.io** | **ได้ — ดีที่สุดถ้าอยากให้ข้อมูลอยู่ถาวร** | มี volume ถาวรในแพ็กเกจฟรี แต่ต้องผูกบัตรเครดิต |
| **เครื่องตัวเอง + Cloudflare Tunnel** | **ได้ — ไม่ต้องสมัครอะไรเลย** | เหมาะกับเดโมชั่วคราว |
| **Vercel / Netlify** | **ใช้ไม่ได้** | ดูเหตุผลด้านล่าง |

### ทำไม Vercel ถึงใช้กับโครงงานนี้ไม่ได้

ไม่ใช่เพราะตั้งค่าผิด แต่เพราะสถาปัตยกรรมคนละแบบกันเลย

1. **Vercel เป็นแบบ serverless** คือรันโค้ดเฉพาะตอนมีคนเรียก แล้วดับไป
   แต่เว็บนี้ต้องเปิด HTTP server ค้างไว้ตลอดเวลา (`server/server.js`)
2. **เขียนไฟล์ถาวรไม่ได้** แต่เว็บนี้เก็บข้อมูลด้วย SQLite ซึ่งเป็นไฟล์
   ข้อมูลผู้ใช้ แผนการอ่าน และเป้าหมายทั้งหมดอยู่ในไฟล์นั้น
3. **หน่วยความจำไม่ถูกเก็บต่อเนื่อง** ตัวนับ rate limit ที่ใช้กันการเดารหัสผ่าน
   (`server/security/ratelimit.js`) เก็บใน `Map` ในหน่วยความจำ ถ้ารันแบบ serverless
   ตัวนับจะรีเซ็ตแทบทุกคำขอ ทำให้ระบบกันเดารหัสผ่าน**ใช้การไม่ได้เลย**

ถ้าจะขึ้น Vercel จริง ๆ ต้องรื้อเปลี่ยนฐานข้อมูลเป็นบริการภายนอก (เช่น Postgres)
และเปลี่ยนระบบ rate limit ไปใช้ที่เก็บภายนอกด้วย ซึ่งเกินขอบเขตโครงงานและทำให้ไม่ฟรีอีกต่อไป

---

## ทางเลือกที่ 1 — Render (แนะนำสำหรับส่งงาน)

### ก่อนเริ่ม: ตรวจว่าบัญชีไม่ถูกระงับ

เข้า <https://dashboard.render.com/billing>

ถ้าเห็นข้อความว่าบัญชีถูกระงับ (suspended) หรือ service เดิมขึ้นสถานะ **Suspended**
จะ**สร้าง service ใหม่ไม่ได้เลย** ต้องแก้เรื่องค่าใช้จ่ายให้เรียบร้อยก่อน
เช่น จ่ายยอดค้าง หรือลบ service เก่าที่ใช้แพ็กเกจเสียเงินอยู่

> วิธีเช็คเร็ว ๆ: เข้า <https://dashboard.render.com/> ถ้า service เดิมขึ้นป้ายสีแดงว่า
> Suspended พร้อมคำว่า billing แปลว่าติดปัญหานี้อยู่

### วิธีที่ 1ก — ใช้ Blueprint (ง่ายที่สุด กดไม่กี่ที)

โครงงานนี้มีไฟล์ `render.yaml` เตรียมไว้แล้ว Render จะอ่านค่าทั้งหมดเอง

1. เข้า <https://dashboard.render.com/blueprints>
2. กด **New Blueprint Instance**
3. เลือก repository **GoldFish1705/Highschool**
   (ถ้าไม่เห็นในรายการ ให้กด *Configure account* เพื่ออนุญาตให้ Render เข้าถึง repo ก่อน)
4. Render จะอ่าน `render.yaml` แล้วแสดงค่าที่จะตั้งให้ดู — กด **Apply**
5. รอสักครู่ แล้วจะได้ URL หน้าตาประมาณ `https://study-planner.onrender.com`

Blueprint จะตั้งค่าเหล่านี้ให้อัตโนมัติ รวมถึง**สุ่มกุญแจลับ `SESSION_SECRET` ให้เอง**
ผู้ใช้จึงไม่ต้องสร้างเอง และค่านั้นจะไม่ปรากฏใน git เลย

### วิธีที่ 1ข — ตั้งค่าเองทีละช่อง

ถ้าไม่อยากใช้ Blueprint ให้กด **New +** → **Web Service** แล้วกรอกตามนี้

| ช่อง | ค่าที่ต้องใส่ |
|---|---|
| Repository | `GoldFish1705/Highschool` |
| Branch | `claude/webapp-project-security-e67u3s` |
| Region | Singapore |
| Runtime | Node |
| Build Command | `echo "ไม่ต้อง build"` |
| Start Command | `node --no-warnings --max-old-space-size=128 server/server.js` |
| Instance Type | Free |

แล้วเพิ่ม Environment Variables ตามนี้

| Key | Value | ทำไมต้องมี |
|---|---|---|
| `NODE_VERSION` | `22` | **สำคัญที่สุด** ถ้าไม่ตั้ง จะพังทันที (ดูหัวข้อแก้ปัญหา) |
| `NODE_ENV` | `production` | เปิดโหมดใช้งานจริง |
| `TRUST_PROXY` | `1` | อ่าน IP จริงของผู้ใช้จากหลัง proxy |
| `SECURE_COOKIES` | `1` | บังคับให้คุกกี้ส่งผ่าน HTTPS เท่านั้น |
| `UV_THREADPOOL_SIZE` | `2` | ประหยัดแรม (148 MB → 110 MB) |
| `MALLOC_ARENA_MAX` | `2` | ประหยัดแรม |
| `SESSION_SECRET` | กด **Generate** ให้ Render สุ่มให้ | กุญแจลับของระบบ session |

**ไม่ต้องตั้ง `APP_ORIGIN`** เพราะ Render ตั้ง `RENDER_EXTERNAL_URL` ให้อยู่แล้ว
และ `server/config.js` อ่านค่านั้นมาใช้ตรวจ CSRF ให้เองโดยอัตโนมัติ

### ข้อจำกัดของแพ็กเกจฟรี Render ที่ต้องรู้ก่อนใช้จริง

1. **ดิสก์ไม่ถาวร** — ข้อมูลทั้งหมด (บัญชีผู้ใช้ แผนการอ่าน เป้าหมาย)
   **จะถูกล้างทุกครั้งที่ deploy ใหม่หรือเครื่องรีสตาร์ท**
2. **เครื่องจะหลับเมื่อไม่มีคนใช้ราว 15 นาที** — คนที่เข้ามาหลังจากนั้นจะรอราว 1 นาทีให้เครื่องตื่น

**สรุป: เหมาะกับการนำเสนอโครงงานและส่งลิงก์ให้ครูดู แต่ยังไม่เหมาะเก็บข้อมูลจริงระยะยาว**
ตอนนำเสนอแนะนำให้สมัครบัญชีสาธิตสด ๆ หน้าห้อง จะได้ไม่เจอปัญหาข้อมูลหาย

---

## ทางเลือกที่ 2 — Fly.io (ข้อมูลอยู่ถาวร)

เหมาะเมื่ออยากให้ข้อมูลไม่หาย มีไฟล์ `fly.toml` เตรียมไว้แล้ว
**ข้อควรทราบ: Fly.io ต้องผูกบัตรเครดิตเพื่อยืนยันตัวตน แม้จะใช้แพ็กเกจฟรี**

```bash
# 1. ติดตั้งเครื่องมือแล้วเข้าสู่ระบบ
curl -L https://fly.io/install.sh | sh
fly auth signup

# 2. แก้ชื่อแอปในไฟล์ fly.toml ให้เป็นชื่อที่ยังไม่มีคนใช้
#    เช่น app = "study-planner-sk2569"

# 3. สร้างแอป (ยังไม่ deploy)
fly launch --no-deploy --copy-config

# 4. สร้าง volume ถาวรไว้เก็บฐานข้อมูล
fly volumes create study_planner_data --size 1 --region sin

# 5. ตั้งกุญแจลับ
fly secrets set SESSION_SECRET="$(openssl rand -base64 48)"

# 6. นำขึ้นเซิร์ฟเวอร์
fly deploy
```

ไม่ต้องตั้ง `APP_ORIGIN` เช่นกัน เพราะระบบอ่านจาก `FLY_APP_NAME` ที่ Fly ตั้งให้อัตโนมัติ

---

## ทางเลือกที่ 3 — รันบนเครื่องตัวเอง แล้วเปิดให้คนอื่นเข้าได้ชั่วคราว

ทางนี้**ไม่ต้องสมัครอะไรเลย ไม่ต้องใช้บัตรเครดิต** และข้อมูลอยู่ในเครื่องเราเอง
เหมาะกับตอนนำเสนอหน้าห้อง หรือให้เพื่อนทดลองใช้ชั่วคราว

```bash
# หน้าต่างที่ 1 — เปิดเว็บไซต์
git clone https://github.com/GoldFish1705/Highschool.git
cd Highschool
npm start
```

```bash
# หน้าต่างที่ 2 — เปิดให้เข้าจากอินเทอร์เน็ตได้
# ติดตั้ง cloudflared จาก https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/downloads/
cloudflared tunnel --url http://localhost:3000
```

`cloudflared` จะพิมพ์ URL แบบ `https://xxxx-xxxx.trycloudflare.com` ออกมาให้ ส่งลิงก์นั้นให้เพื่อนหรือครูได้เลย

**ข้อควรระวัง:** ปิดหน้าต่างเมื่อไหร่ ลิงก์ก็ใช้ไม่ได้ทันที และเนื่องจากเป็น URL ชั่วคราว
ระบบจะยังไม่รู้จัก origin นี้ ให้เพิ่มตัวแปรตอนเปิดเซิร์ฟเวอร์ด้วย

```bash
APP_ORIGIN="https://xxxx-xxxx.trycloudflare.com" SECURE_COOKIES=1 TRUST_PROXY=1 npm start
```

---

## แก้ปัญหาที่พบบ่อย

### เปิดเว็บแล้วขึ้น error ว่าหาโมดูล `node:sqlite` ไม่เจอ

```
Error: Cannot find module 'node:sqlite'
```

**สาเหตุ:** โฮสต์ใช้ Node เวอร์ชันต่ำกว่า 22.5 (ส่วนใหญ่ตั้งค่าเริ่มต้นเป็น Node 20)
เพราะ `node:sqlite` เพิ่งมีใน Node 22.5

**วิธีแก้:** ตั้ง environment variable `NODE_VERSION` เป็น `22`
(โครงงานมีไฟล์ `.node-version` กำกับไว้อีกชั้นแล้ว แต่โฮสต์บางเจ้าไม่อ่านไฟล์นี้)

### เข้าเว็บได้ แต่เข้าสู่ระบบไม่ได้ ขึ้นว่า "คำขอถูกปฏิเสธเพื่อความปลอดภัย"

**สาเหตุ:** ระบบมองว่า origin ของคำขอไม่ตรงกับเว็บไซต์ (การป้องกัน CSRF ทำงาน)

**วิธีแก้:** ตั้ง `APP_ORIGIN` ให้ตรงกับ URL จริง เช่น `https://study-planner.onrender.com`
(ห้ามใส่ `/` ปิดท้าย)

ตรวจได้จาก log ตอนเปิดเซิร์ฟเวอร์ ถ้าตั้งถูกจะขึ้นบรรทัดนี้

```
[server] origin ที่ใช้ตรวจ CSRF: https://study-planner.onrender.com
```

ถ้าขึ้นเป็นคำเตือนว่ายังไม่ได้ตั้ง แปลว่าโฮสต์ไม่ได้บอก URL มาให้ ต้องตั้งเอง

### ล็อกอินแล้วหลุดทันที หรือข้อมูลหายหมด

**สาเหตุ:** อยู่บนแพ็กเกจฟรีของ Render ซึ่งดิสก์ไม่ถาวร ข้อมูลถูกล้างเมื่อเครื่องรีสตาร์ท

**วิธีแก้:** ถ้าต้องการให้ข้อมูลอยู่ถาวร ต้องย้ายไป Fly.io (มี volume) หรือเพิ่ม Persistent Disk
ของ Render ซึ่งเป็นบริการแบบเสียเงิน

### สร้าง service ใหม่บน Render ไม่ได้

ถ้ากดสร้างแล้วขึ้น error ให้ตรวจที่ <https://dashboard.render.com/billing> ก่อน
บัญชีที่ถูกระงับเรื่องค่าใช้จ่ายจะสร้าง service ใหม่ไม่ได้เลย

---

## ตรวจว่า deploy สำเร็จจริงหรือยัง

เมื่อได้ URL มาแล้ว ให้ลองตามนี้

1. เปิด `https://<url ของคุณ>/login` → ต้องเห็นหน้าเข้าสู่ระบบภาษาไทย
2. สมัครสมาชิกใหม่ → ต้องเข้าหน้าหลักได้และเห็นรายวิชาเริ่มต้น 5 วิชา
3. เพิ่มแผนการอ่าน 1 รายการ แล้วกดติ๊กว่าอ่านแล้ว → ตัวเลขในหน้าหลักต้องเปลี่ยน
4. ดู log ของโฮสต์ ต้องเห็น 3 บรรทัดนี้ และ**ต้องไม่มีคำเตือนเรื่อง APP_ORIGIN**

```
[server] เว็บไซต์วางแผนการอ่านหนังสือพร้อมใช้งานที่ http://0.0.0.0:10000
[server] โหมด: production | ฐานข้อมูล: /opt/render/project/src/data/study-planner.db
[server] origin ที่ใช้ตรวจ CSRF: https://<url ของคุณ>
```
